// == Import npm

/*


import React from 'react';
import PropTypes from 'prop-types';
import { Image, TouchableOpacity,Text } from 'react-native';

import { useNavigation } from '@react-navigation/native';

// Import routes
import { PAGE_ETAPE } from '@components/navigations/routes';


// Import components
import TextCustom from '@components/partials/TextCustom';

// == Import Style
import { liste } from '@styles/Themes';
import color from '@styles/Themes/variables/color';


import mapIcon from '@styles/images/icon-map-rose/icon-map-rose.png';
  
 


// == Composant
const GotoMarkerMap = ({ marker,   setMarkerSelected}) => {
  const navigation = useNavigation();
  
  const showMarkeronMap = () => {
    setMarkerSelected(marker);
    navigation.navigate(PAGE_ETAPE);
  };

  return (
  <TouchableOpacity onPress={showMarkeronMap} ><Image source={mapIcon}  /></TouchableOpacity>
);
}
// == Validation
GotoMarkerMap.propTypes = {
 marker: PropTypes.object.isRequired,
  setMarkerSelected: PropTypes.func.isRequired,
};

// == Export
export default React.memo(GotoMarkerMap);

*/
